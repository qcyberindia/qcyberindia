// UNUSED — kept only so this filename doesn't silently reappear as a stray import.
// The (marketing) route group's own layout.tsx now handles Nav/PromiseBanner/Footer
// for all marketing pages, and /qbids sits outside that group so it never inherits
// them. This wrapper is no longer needed and is safe to delete.
export {};
